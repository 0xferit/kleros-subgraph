{
  "name": "kleros",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ quilldata/kleros",
    "create-local": "graph create --node http://localhost:8020/ quilldata/kleros",
    "remove-local": "graph remove --node http://localhost:8020/ quilldata/kleros",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 quilldata/kleros"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.15.0",
    "@graphprotocol/graph-ts": "^0.15.1"
  }
}
